<table>
  <tr>
    <th>Journals</th>
  </tr>
  <% @journals.each do |journal| %>
    <tr>
      <td><%= journal.title %></td>
    </tr>
  <% end %>
</table>
<%= button_to 'Create New Journal', new_journal_path, method: 'get' %>
